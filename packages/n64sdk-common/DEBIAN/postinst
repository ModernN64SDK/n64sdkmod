#!/bin/sh

echo "\033[1;33mCaution: The N64_CC, N64_AS etc environment variables are set to the mips-n64 GCC toolchain by default. Consult https://github.com/CrashOveride95/n64sdkmod/blob/master/INSTALL.md for information on building these if your package manager does not provide this GCC toolchain.\033[0m"
